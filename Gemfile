source 'https://rubygems.org'

gem 'rails'
gem 'gem-wrappers'
gem 'sprockets-rails'
gem 'pg'
gem 'puma'

# Client
gem 'httparty' # per le request
gem 'json-api-vanilla' # Per il parsing della

# Server
gem 'active_model_serializers' # Per la costruzione della json-api
